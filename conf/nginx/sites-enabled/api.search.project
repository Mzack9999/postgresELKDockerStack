server {
  listen ${NGINX_PORT};
  listen [::]:${NGINX_PORT};

  server_name ${API_SEARCH_HOST};

  root ${NGINX_SOURCE_PATH}${API_SEARCH_SOURCE_PATH};

  location / {
        try_files $uri $uri/ =404;
  }
}
