version: '3.3'

services:
  elastic-search:
    container_name: elastic-search
    image: docker.elastic.co/elasticsearch/elasticsearch:5.6.3
    restart: always
    environment:
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    volumes:
      - esdata:/usr/share/elasticsearch/data
    ulimits:
      memlock:
        soft: -1
        hard: -1

  nginx:
    container_name: nginx
    image: nginx:latest
    restart: always
    environment:
      API_REST_HOST: ${API_REST_HOST}
      API_SEARCH_HOST: ${API_SEARCH_HOST}
      API_REST_SOURCE_PATH: ${API_REST_SOURCE_PATH}
      API_SEARCH_SOURCE_PATH: ${API_SEARCH_SOURCE_PATH}
      WEB_SOURCE_PATH: ${WEB_SOURCE_PATH}
      WEB_HOST: ${WEB_HOST}
      WEB_HOST_WWW: ${WEB_HOST_WWW}
      NGINX_PORT: ${NGINX_PORT}
      NGINX_CONFIG_FILE_PATH: ${NGINX_CONFIG_FILE_PATH}
      NGINX_SITES_ENABLED_DIRECTORY: ${NGINX_SITES_ENABLED_DIRECTORY}
      NGINX_SOURCE_PATH: ${NGINX_SOURCE_PATH}
    ports:
      - ${HOST_NGINX_PORT}:${NGINX_PORT}
    volumes:
      - ./entrypoint/nginx.sh:/entrypoint.sh
      - ./conf/nginx/nginx.conf:/tmp/${NGINX_CONFIG_FILE_PATH}
      - ./conf/nginx/sites-enabled:/tmp/${NGINX_SITES_ENABLED_DIRECTORY}
      - ./src/http:${NGINX_SOURCE_PATH}
    entrypoint: /entrypoint.sh

  postgres:
    container_name: postgres
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGDATA: ${POSTGRES_DATA_DIR}

volumes:
  esdata:
    driver: local
